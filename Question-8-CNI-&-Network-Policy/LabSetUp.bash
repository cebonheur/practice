#!/bin/bash
Q_NUM="q8"
echo "⚠️ WARNING: This lab involves installing a CNI (Flannel/Calico)."
echo "⚠️ Installing a CNI on a cluster that already has one will likely BREAK cluster networking."
echo "⚠️ It is recommended to run this lab in a fresh environment or be prepared to rebuild the cluster."
echo
echo "# For this lab you can just use the killercoda playground as it is, no adjustments needed"
echo "# If you create resources, please use names prefixed with $Q_NUM- or use namespace $Q_NUM-default"
